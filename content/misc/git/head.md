+++
title = "HEAD"
front = "HEAD means \"where you currently are\" in the repository history."
category = "branching"
difficulty = "beginner"
weight = 105
+++

Most of the time, `HEAD` points to the current branch, and the branch points to the latest commit on that branch.

Real-world example: if you are on `main`, new commits move `main` forward, and `HEAD` follows along because it points at `main`.
