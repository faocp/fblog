+++
title = "pip"
front = "`pip` installs Python packages from package indexes like PyPI."
category = "tooling"
difficulty = "beginner"
weight = 200
+++

Examples:

```sh
pip install requests
pip freeze > requirements.txt
```

Real-world example: use `requirements.txt` or another dependency file so another developer can install the same packages your project needs.
