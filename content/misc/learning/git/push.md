+++
title = "Push"
front = "Sends your local commits to a remote."
category = "remote"
difficulty = "beginner"
weight = 210
+++

```sh
git push                       # if upstream is set
git push -u origin feature-x   # first push of a new branch
```

`-u` sets the upstream so future `push`/`pull` work without arguments. After this, your branch shows up on GitHub.
