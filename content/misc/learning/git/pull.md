+++
title = "Pull"
front = "Fetches remote changes and merges them into your branch."
category = "remote"
difficulty = "beginner"
weight = 130
+++

```sh
git pull
git pull --rebase     # avoids extra merge commits
```

Run before starting work each day. `--rebase` keeps history cleaner on shared branches.
