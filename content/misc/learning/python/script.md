+++
title = "Script"
front = "A Python script is a `.py` file containing code you can run repeatedly."
category = "basics"
difficulty = "beginner"
weight = 30
+++

Example:

```sh
python hello.py
```

Scripts are good for automating tasks, building command-line tools, or starting applications.

Real-world example: a `resize_images.py` script can process a whole folder the same way every time.
