+++
title = "git log"
front = "`git log` shows commit history: who changed what, when, and with which message."
category = "commands"
difficulty = "beginner"
weight = 70
+++

`git log --oneline --graph --decorate` gives a compact view of commits, branches, and merges.

Real-world example: use the log to find when a feature was introduced, who worked on it, or which commit might have caused a bug.
