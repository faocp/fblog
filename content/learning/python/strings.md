+++
title = "Strings"
front = "A string, or `str`, stores text."
category = "data"
difficulty = "beginner"
weight = 60
+++

Examples:

```py
message = "hello"
message.upper()
len(message)
```

Strings are immutable, meaning operations create new strings rather than changing the original one.
