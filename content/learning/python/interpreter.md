+++
title = "Interpreter"
front = "Python code is run by an interpreter, which reads and executes your program."
category = "basics"
difficulty = "beginner"
weight = 10
+++

The interpreter is the `python` or `python3` program installed on your machine.

Real-world example: when you run `python app.py`, Python reads `app.py`, executes statements from top to bottom, and reports errors if it finds invalid syntax or runtime problems.
