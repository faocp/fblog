+++
title = "Repository"
front = "A Git repository is a project folder where Git stores history and tracks changes."
category = "basics"
difficulty = "beginner"
weight = 10
+++

A repository usually contains your project files plus a hidden `.git/` directory.

Real-world example: when you run `git init` inside a folder, Git starts recording snapshots for that project. If you delete `.git/`, the files remain, but the Git history is gone.
