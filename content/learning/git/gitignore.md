+++
title = ".gitignore"
front = "A `.gitignore` file tells Git which untracked files it should usually ignore."
category = "basics"
difficulty = "beginner"
weight = 190
+++

Use it for generated files, dependencies, local editor settings, build output, and secrets that should not be committed.

Real-world example: ignore `node_modules/`, `.env`, and `public/` so your commits contain source files rather than machine-specific or generated files.
