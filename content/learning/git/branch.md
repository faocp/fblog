+++
title = "Branch"
front = "A branch is a movable pointer to a commit, marking the tip of a line of work."
category = "branching"
difficulty = "beginner"
weight = 80
+++

Branches let you work on features, fixes, or experiments without immediately changing the main line of development.

Real-world example: create `feature/search` to build search while `main` stays stable and deployable.
