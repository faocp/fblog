+++
title = "Commit"
front = "A commit is a saved snapshot of your project's tracked files, built from what you staged, with a message explaining why."
category = "basics"
difficulty = "beginner"
weight = 40
+++

Think of a commit as a checkpoint you can inspect, compare, share, or return to later.

Example:

```sh
git add README.md
git commit -m "Explain local setup"
```

A good commit message says what changed and, when helpful, why it changed.
