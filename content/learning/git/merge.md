+++
title = "Merge"
front = "A merge combines changes from one branch into another."
category = "branching"
difficulty = "beginner"
weight = 110
+++

When histories have diverged, Git may create a merge commit that ties both lines of work together.

Real-world example: after finishing `feature/login`, switch to `main` and run `git merge feature/login` to bring that work into the main branch.
