+++
title = "git status"
front = "`git status` shows what changed, what is staged, and what branch you are on."
category = "commands"
difficulty = "beginner"
weight = 50
+++

This is the safest command to run often because it does not change anything.

Real-world example: before committing, run `git status` to check whether you accidentally staged a debug file, secret, or unrelated change.
