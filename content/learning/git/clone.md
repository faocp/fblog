+++
title = "Clone"
front = "`git clone` copies an existing repository, including its history, to your machine."
category = "remotes"
difficulty = "beginner"
weight = 130
+++

Cloning is usually how you start working on an existing project.

Example:

```sh
git clone https://github.com/user/project.git
```

After cloning, you have the files, commit history, branches, and a default remote named `origin`.
