+++
title = "Stash"
front = "`git stash` temporarily shelves uncommitted changes so you can work with a clean tree."
category = "undo"
difficulty = "intermediate"
weight = 230
+++

Use `git stash push` to save changes and `git stash pop` to reapply them.

Real-world example: you are halfway through a feature when an urgent bug appears. Stash your work, switch branches, fix the bug, then pop the stash to continue.
