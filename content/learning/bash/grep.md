+++
title = "grep"
front = "`grep` searches text for lines matching a pattern."
category = "commands"
difficulty = "beginner"
weight = 140
+++

Examples:

```sh
grep "TODO" *.md
grep -R "function" .
grep -i "error" server.log
```

`-R` searches recursively and `-i` ignores case.
