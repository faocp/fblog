+++
title = "Shell"
front = "A shell is a program that reads commands and asks the operating system to run them."
category = "basics"
difficulty = "beginner"
weight = 10
+++

Bash is one kind of shell. Other common shells include zsh, fish, and sh.

Real-world example: when you type `ls` and press Enter, the shell finds the `ls` program, runs it, and prints the result in your terminal.
