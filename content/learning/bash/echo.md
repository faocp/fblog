+++
title = "echo"
front = "`echo` prints text or variable values to the terminal."
category = "commands"
difficulty = "beginner"
weight = 70
+++

Examples:

```sh
echo hello
echo "$HOME"
```

Real-world example: use `echo` while debugging a script to check which value a variable contains before running a command with it.
