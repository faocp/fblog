+++
title = "cd"
front = "`cd` changes the current directory."
category = "navigation"
difficulty = "beginner"
weight = 40
+++

Examples:

```sh
cd projects
cd ..
cd ~
cd -
```

`..` means parent directory, `~` means your home directory, and `cd -` jumps back to the previous directory.
