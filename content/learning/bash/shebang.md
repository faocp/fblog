+++
title = "Shebang"
front = "A shebang tells the system which interpreter should run a script."
category = "scripting"
difficulty = "beginner"
weight = 230
+++

It appears on the first line of a script:

```sh
#!/usr/bin/env bash
```

Real-world example: with a shebang and execute permission, you can run `./deploy.sh` instead of typing `bash deploy.sh`.
