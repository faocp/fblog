+++
title = "cat and less"
front = "`cat` prints a file; `less` opens a scrollable view of a file."
category = "commands"
difficulty = "beginner"
weight = 80
+++

Use `cat` for short files and `less` for longer files.

Examples:

```sh
cat README.md
less server.log
```

Real-world example: use `less` to inspect logs without flooding your terminal. Press `/` to search and `q` to quit.
