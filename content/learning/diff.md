+++
title = "git diff"
front = "`git diff` shows the exact line-by-line changes between versions."
category = "commands"
difficulty = "beginner"
weight = 60
+++

By default, `git diff` shows unstaged changes. Use `git diff --staged` to review what is ready for the next commit.

Real-world example: before committing, scan the diff to catch leftover `console.log`, temporary comments, or accidental formatting changes.
