+++
title = "git switch"
front = "`git switch` moves you to another branch, or creates and moves to a new branch."
category = "commands"
difficulty = "beginner"
weight = 100
+++

Examples:

```sh
git switch main
git switch -c feature/login
```

Real-world example: switch from `main` to a feature branch before starting work so your changes are isolated from production-ready code.
